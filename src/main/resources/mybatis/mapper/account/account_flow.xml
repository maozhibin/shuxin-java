<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baoquan.shuxin.dao.account.AccountFlowDao" >

  <select id="querAccountFlowInfo" resultMap="com.baoquan.shuxin.model.account.AccountFlow">
    SELECT 	id,user_id, type,finish_time FROM user_money_log  WHERE  user_id = #{userId} and type = #{type} and finish_time =#{}
    BETWEEN dateline =#{dateline} AND  finish_time=#{finishTime}
  </select>




</mapper>